<div class="camel-spec">
  <div class="pic-and-map">
  <div class="product-image">
    <%= cl_image_tag @camel.photo, height: 300, width: 400, crop: :fill %>
  </div>
  <div id="map"
    data-markers="<%= @marker.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
  </div>

  <div class="bookingform">
    <h1> <%= @camel.name %></h1>
    <p class="camel-description"><%= @camel.description %></p>
    <p class="camel-cat-<%= @camel.category %>">Category<br><span><%= "#{@camel.category}" %></span> </p>
    <p class="camel-loc-<%= @camel.location %>">Location<br><span><%= "#{@camel.location}" %></span></p>

    <div class="price">
      <p id="vat"><%= "Price/day - VAT included" %></p>
      <h3><%= "#{@camel.price}$  " %></h3>
    </div>

    <%= simple_form_for [@camel, @booking] do |f| %>
    <%= f.input :date, label: false, placeholder: "Select your date", as: :string, required: false, input_html: {class: "datepicker"} %>
    <%= f.button :submit, "Create your booking", class: "btn" %>
    <% end %>

    <% if policy(@camel).destroy? %>
    <div class= "remove">
      <%= link_to camel_path(@camel), method: :delete, class: "btn btn-delete" do %>
      <i class="fas fa-trash"></i>
      <% end %>
    </div>
    <% end %>
    <p id="show-text"><%= link_to 'Show all camels available', camels_path%></p>
  </div>

</div>



<h6>AirBosseN'Bosse, ‌I‌n‌c‌.‌, is a French online marketplace and micro-mobility service brokerage company based in Villa Gaudelet, France. Members can use the service to arrange or offer alternatives to e-scooters, primarily camels. If our fleet is fully booked, a dromedary will be offered.</h6>
